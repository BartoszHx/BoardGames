using System;
using System.Collections.Generic;
using System.Text;

namespace BoardGamesClient
{
    public enum ServiceResponseStatus
    {
        Ok = 0,
        Error = 1,
        Cancel = 2,
    }
}
